<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".VerifyActivity"></activity>
        <activity android:name=".AllProfilesAdapter" />
        <activity android:name=".AllProfilesActivity" />
        <activity android:name=".AllPostsAdapter" />
        <activity android:name=".AllPostsActivity" />
        <activity android:name=".MyPostActivity" />
        <activity android:name=".PostActivity" />
        <activity android:name=".EditActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>